import { test } from "@playwright/test";

/** PZ 12.6. */
test.skip("Finance kreira fakturu i pokreće handoff batch", async () => {
  // pnpm seed + test scenario; mock SFTP target preko HANDOFF_DIR env.
});
