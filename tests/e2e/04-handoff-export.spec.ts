import { test, expect } from "@playwright/test";

/** PZ 12.6 + 15.4. */
test.skip("Finance generiše batch izvoz (ERP)", async () => {
  // Implementiraj sa Finance nalogom; traži 2FA; mock TOTP u CI-u.
});
