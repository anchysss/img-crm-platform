# M11 — Hardening, performanse, security, UAT

## Zadaci
- [x] XLSX eksport kroz ExcelJS (dinamički import, lazy chunk) + CSV zadržan kao alternative.
- [x] PDF: printable invoice view `/invoices/[id]` sa `@media print` CSS → browser "Save as PDF".
- [x] Next.js middleware: auth guard na sve `/dashboard`, `/pipeline`, `/opportunities`, itd. rute + fallback CSP.
- [x] Session idle timeout: NextAuth `session.maxAge` iz `SESSION_IDLE_TIMEOUT_MINUTES` env (default 30 min).
- [x] Retention cleanup cron `retention-cleanup.ts` (AuditLog 24 mes., PiiPristup 24 mes., Notifikacije 90 dana).
- [x] Dependabot config (npm weekly + github-actions monthly + groups za prod/dev).
- [x] Security workflow (`npm audit` fail na high/critical, ZAP baseline stub za staging).
- [x] k6 load test `tests/perf/50-concurrent.js` (50 VUs, P95<300ms threshold per PZ 5).
- [x] UAT scenario verifier `scripts/uat/verify-scenarios.ts` (12 automated checks).
- [x] Proširen `package.json` sa job scripts (`job:retention`, `job:stale-opps`, `job:communal-expiring`, `job:release-holds`, `job:advance-campaigns`, `uat:verify`).

## Scope-cut (out of current work)
- CSRF double-submit cookie pattern za tRPC (NextAuth JWT već štiti session cookie, dodatni layer u sledećem sprintu).
- Sentry + OTEL konfiguracija (env varijable postavljene, init kod u deploy fazi).
- Backup/restore playbook test (zahteva managed DB snapshot mehanizam na staging-u).
- `docker-compose.prod.yml` (zahteva izbor hosting provajdera — otvoreno pitanje br. 5).

## DoD
- [x] Typecheck clean, build green (24 rute + middleware 47.9 kB).
- [x] 25/25 automatskih testova prolazi (14 unit + 11 integration).
- [x] UAT verifier 12/12 prolazi.
- [x] Middleware auth guard verifikovan kroz curl: `/dashboard` bez tokena → 307 na `/login?callbackUrl=/dashboard`.
- [x] Retention cron izvršen na live DB (audit=0, pii=0, notif=0 jer je baza sveža).
- [ ] OWASP ZAP staging scan — traži staging environment.
- [ ] k6 sustained run — traži izolovano okruženje (ne blokirajuće za M11 prijem).
- [ ] UAT izveštaj potpisan od strane Naručioca.
