# M6 — Dokumenti (predračun/faktura) + finansijski handoff

## Zadaci
- [x] Prisma: Dokument + DokumentStavka + NumeracijaSkok + HandoffBatch + HandoffZapis.
- [x] ADR-0003 invoice numeration (atomski sekvenca per pravnoLice+tip+godina).
- [x] Router `invoices` (list/create/storno) + `handoff` (triggerBatch/list).
- [x] Handoff servis: format per država (SRB_SEF / MNE_STD / HRV_FINA / BIH_STD).
- [x] Storno kreira novi STORNO dokument sa negativnim iznosima + linkuje na original.
- [x] UI: dokumenti lista sa stornom + novi dokument forma (multi-stavka sa auto-računom podzbira/PDV-a/ukupnog) + handoff panel (trigger batch + istorija).

## DoD
- Integration test `invoice-numeration.test.ts`: atomska sekvenca sa paralelnim pozivima prolazi.
- Integration test `handoff-format.test.ts`: sva 4 formata wrap-uju envelope korektno.
- Handoff fajl se zapiše u `/tmp/img-handoff/<PL_KOD>/handoff-<PL_KOD>-<timestamp>.json`.
- Storno validacija: ne može se stornirati već stornirani dokument.
