# M7 — Dashboard „Danas" + KPI + izveštaji

## Zadaci
- [x] Dashboard router `today`: KPI (open count, weighted pipeline, forecast month, win rate 90d, closed won mesec) + „vruće stavke" (zapelo > 14 dana).
- [x] Reports router: winRate (sa by partner type), inventoryUtilization, topPartners, aging buckets (0–30 / 31–60 / 61–90 / 90+).
- [x] Pipeline funnel agregacija (count + weighted per stage).
- [x] UI: Dashboard sa KPI karticama + tabelom vruće stavke.
- [x] UI: Reports sa 5 sekcija + CSV export po svakom (PZ 4.13).
- [x] Export helper `src/lib/export.ts` (UTF-8 BOM za Excel-compat).

## Scope-cut (u M11)
- XLSX + PDF eksport (sada: CSV koji Excel otvara kao ugrađen BOM).

## DoD
- Dashboard SalesRep vidi samo svoje (filter `vlasnikId === korisnikId`).
- Forecast se računa po trenutnom mesecu, win rate na 90d close window.
- Utilization iskorišćenje = zauzetost × 100 / (pozicija × dana).
