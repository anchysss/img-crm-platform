# M10 — Notifikacije + podsetnici + dodatni jezici

## Zadaci
- [x] Notifikacije service (vidi M9).
- [x] Sidebar notifications badge (unread count, auto-refresh).
- [x] In-app notifikacije UI (lista + mark-read + bulk mark-all).
- [x] Email notifikacije — slanje preko SMTP (hookovi postavljeni, konkretni SMTP u produkciji; lokalno MailHog).
- [x] i18n: sr-Latn, sr-Cyrl, hr, bs, cnr, en.
- [x] Request config detektuje locale kroz cookie `img.locale` ili `Accept-Language` header.
- [x] API `/api/locale` POST za promenu jezika (session cookie).
- [x] Profile page: language switcher.

## Scope-cut u M11
- Push notifikacije (mobile) — Phase 2.
- Puni prevod svih stringova (trenutno: nav + dashboard KPI + opportunity stage labels; ostalo engleski u UI-ju dok se ne prevede).

## DoD
- Promena jezika u Profile → reload → naziv nav-a se menja.
- Cron jobs pokreću se kroz `tsx scripts/jobs/...`.
