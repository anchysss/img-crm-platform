# M2 — Partneri + kontakti + aktivnosti

## Zadaci
- [x] Prisma modeli: Partner, Kontakt, Aktivnost.
- [x] tRPC router `partners` (list/byId/create/update/softDelete) + `contacts` (listByPartner/create/update) + `activities` (listByOpportunity/listByPartner/create).
- [x] Tenant izolacija + audit log + PII access log.
- [x] Full-text pretraga po partneru (naziv contains insensitive).
- [x] UI: lista partnera sa filterima, create modal, detail sa kontaktima i aktivnostima.
- [x] UI za dodavanje primarnog kontakta (exclusivity transaction).

## DoD
- Seed: 30 partnera (4 po MNE, SRB, HRV, BIH) + 60 kontakata, idempotentno.
- E2E manualno: kreiran TEST Partner, audit log zabeležio `CREATE Partner`.
- RBAC: SalesRep kreira partnera, ReadOnly nema CREATE permisiju.
