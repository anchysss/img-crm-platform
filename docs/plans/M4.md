# M4 — Vozila + pozicije + MediaBook plan + hold

## Zadaci
- [x] Prisma: Vozilo, Pozicija, Rezervacija (sa status enum HOLD/CONFIRMED/RUNNING/CANCELLED/RELEASED), KomunalnaNaknada.
- [x] Router `vehicles` (list/byId/create/updateStatus/addPosition/addCommunalFee).
- [x] Router `mediabook` (grid/setHold/releaseHold) sa konflikt-check.
- [x] 14-dana default hold expiration, konfigurabilno.
- [x] Cron job `release-expired-holds.ts`.
- [x] UI: vozila lista + create forma, MediaBook grid sa color legend (Slobodno / Hold / Potvrđeno / U realizaciji / Nedostupno).

## DoD
- Integration test `opportunity-lifecycle.test.ts`: HOLD → CONFIRMED on WON, HOLD → RELEASED on LOST — prolazi.
- Konflikt-check sprečava dupli hold iste pozicije/termina.
- Seed: 20 vozila × 4 pozicije = 80 pozicija.
