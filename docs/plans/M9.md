# M9 — GDPR/ZoZLP + audit log + Notifikacije

## Zadaci
- [x] Prisma: AuditLog (append-only), LicniPodatakPristup (read log za PII).
- [x] Helper `src/server/audit.ts`: `audit()` + `logPiiAccess()`.
- [x] Retencija enum (trenutno: retain forever — cron retention u M11).
- [x] GDPR router (rightToAccess / rightToErasure sa pseudonimizacijom).
- [x] UI: admin audit log + admin GDPR sa pretragom po email-u i pseudonimizacija.
- [x] Notifikacije router + service (`notify()` idempotentno po danu, `notifyRoles()` broadcast).
- [x] Trigger notifikacija iz poslovne logike: opportunity assigned, campaign confirmed.
- [x] Cron `notify-stale-opportunities.ts` (OPP_ZAPEO).
- [x] Cron `notify-communal-expiring.ts` (KOMUNALNA_ISTEKLA → Country Manager + Finance + Admin).
- [x] UI: notifikacije lista + mark-read + sidebar badge (auto-refresh 60s).

## DoD
- Cron testiran: 41 notifikacija kreirana za zapele Opportunity-je.
- Pseudonimizacija briše `email`, `telefon`, postavlja `ime = [redacted]` i `pseudonimizovan = true` — audit GDPR_ERASURE ostavljen.
- Audit UI filtriranje po akciji/entitetu/korisniku.
